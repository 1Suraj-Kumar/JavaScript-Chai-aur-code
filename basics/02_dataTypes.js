"use strict"  //treat all js code as newer version

// alert(3 + 3) //we are using nodeJs, not browser

let name = "suraj"
let age = 24
let isLoggedIn = true

// number => 2 to power 53 (range)
// bigint => if range is bigger then number we can use this
// string => " "
// boolean => true/false
// null => standalone value (null means it is empty and it is type of object)
// undefined => (when we not defined variable value)
// symbol => uniqueness

// object

console.log(typeof age);  //if want to know type